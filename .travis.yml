language: python
python: "2.7"

install:
  - pip install -r requirements.txt ansible

script:
  - "python setup.py test"

after_script:
  - config/deploy

notifications:
  irc:
    channels:
      - "irc.freenode.org#takeyourmeds"
    skip_join: true
    use_notice: true
