language: python
python: "2.7"

before_install:
  - sudo apt-get update -qq

install:
  - pip install -r requirements.txt ansible

script:
  - "python setup.py test"

after_script:
  - config/deploy

notifications:
  irc:
    channels:
      - "irc.freenode.org#takeyourmeds"
    skip_join: true
    use_notice: true
