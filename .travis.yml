language: python
python: "2.7"

before_install:
  - sudo apt-get update -qq

install:
  - pip install ansible

script:
  - "python setup.py test"

after_script:
  - config/deploy

notifications:
  irc: "irc.freenode.org#takeyourmeds"
